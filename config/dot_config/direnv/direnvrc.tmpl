# nix-direnv: cached + gc-rooted `use flake`.
{{ if eq .chezmoi.os "darwin" -}}
_nd="{{ .chezmoi.homeDir }}/.local/state/nix/profiles/profile/share/nix-direnv/direnvrc"
{{ else -}}
_nd="/usr/share/nix-direnv/direnvrc"
{{ end -}}
[ -f "$_nd" ] && source "$_nd"
unset _nd
